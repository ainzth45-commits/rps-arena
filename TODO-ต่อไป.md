# 📋 รายการงานที่ต้องทำต่อ — เป่า ยิ้ง ฉุบ! อารีน่า!

> อัปเดต 2026-07-21 (รอบ 2) · เกมเล่นได้ครบวงจรแล้ว · เหลืองานขัดเงา+ฟีเจอร์เสริม
> อ่าน `01-game-design-spec.md` (กติกา) + `04-scene-design.md` (แผนฉาก) ก่อนทำต่อ

---

## ✅ เสร็จแล้ว (เล่นได้จริง)

- **เครื่องยนต์เกม** — เป่ายิ้งฉุบ · ตัวชี้ชุดมูฟ · คะแนน+สตรีค · อันดับ 4 ชั้น · ภาษีของแชมป์ (เทส 110/110)
- **วงจรหลัก** — boot → home → เลือกคน → 📬 recap → เมนูรอบ → ตั้งมูฟ → เลือกคู่ → VS → เลือกมูฟ 30วิ → เป่ายิ้งฉุบ → ผล
- **ดวลนอกรอบ · จบซีซั่น+ประกาศแชมป์ · หน้าประวัติ · ranking (กรองคนไม่แข่ง)**
- **enroll ฟรี** — ตั้งชุดมูฟครั้งแรกไม่ต้องจ่ายเหรียญ (กันเซตฟรีซ้ำ)
- **แก้รูปผู้เล่น** (ลิงก์/ถ่ายสด) · **แถบรอบค้าง** (กันลบคนไม่ได้)
- **งานภาพ** — โลโก้ · ไอคอนแอป · ไอคอนมูฟ · ไอคอนปุ่ม 14 · ตัวละคร 6 · ฉากผล/เอฟเฟกต์ · ซีซั่น
- **ฉากมีชีวิต** — VS (vs-badge+clash-spark) · ฉากผล (คู่ปรับ+confetti)
- **ประกายปะทะในฉากเป่ายิ้งฉุบ** (clash-spark แทน 💥) · **โพเดียมจบซีซั่น** (season-podium + กระดาษฉลอง + ม่านมืดให้อ่านออก)
- **พื้นหลังเฉพาะห้อง** — ห้องปรับมูฟ/ห้องเตรียมตัว/สังเวียนผลดวล (`SceneBackdrop` + class `scene--art`)
- **ฉากปะทะ VS 5 วิ** — ย้ายมาคั่นหลังเลือกมูฟ · สองฝั่งพุ่งเข้าประกบ + จอสั่น + สถิติเจอกัน + แตะข้ามได้
- **ศัพท์เกมใหม่** — ผู้เล่น = ทุกคน · ผู้ท้าชิง = คนจ่ายเหรียญมาท้า (`challenger`) · คู่แข่ง = คนถูกท้า (`opponent`)
  แก้ทั้งข้อความ/โค้ด/เอกสาร + migration เซฟ v1→v2 (`SAVE_VERSION = 2`)

---

## 🔴 งานค้าง — เรียงตาม impact

### กลุ่ม 1: ใส่ asset ที่ sync แล้วแต่ยังไม่ wire
- [x] ~~task #3 — ฉากเป่ายิ้งฉุบใส่ clash-spark~~ ✅
- [x] ~~task #4 — ประกาศแชมป์ใช้ season-podium + confetti~~ ✅
- [x] ~~มงกุฎจ่าฝูง (อันดับ 1) + ไอคอนลูกเต๋าปุ่มสุ่ม~~ ✅
- [ ] เอา **แมว/พนักงาน** ไปใส่หน้าแรก (home) ให้มีชีวิตขึ้น

### กลุ่ม 2: ฟีเจอร์ที่ยังขาด (เกมยังไม่ครบ)
- [x] ~~หน้าบันทึกซีซั่นเก่า~~ ✅ (`SeasonRecordsScene`)
- [x] ~~ปรับเรทคะแนน/เวลา ในตั้งค่า~~ ✅ (`ConfigScene` + `updateConfig` บีบค่าเข้าขอบเขต)
- [x] ~~สอนเล่น~~ ✅ `TutorialScene` 5 หน้า (ปุ่ม "สอนเล่น" ในแถบล่างหน้าแรก)
- [x] ~~ระบบเสียง/ซาวด์~~ ✅ `src/audio/sfx.ts` — สังเคราะห์ด้วย Web Audio ล้วน ไม่มีไฟล์เสียง
      · 11 เสียง: แตะปุ่ม/ยืนยัน/เหรียญ/พุ่งเข้า/กระแทก/นับเป่ายิ้งฉุบ/เปิดมูฟ/ชนะ/แพ้/เสมอ/แชมป์
      · ปุ่มเปิด-ปิดในตั้งค่า (จำค่าใน localStorage) · ปลุก AudioContext ตอนแตะโลโก้ (กฎ iPad)
      · ยังปรับได้: เสียงประกอบตอนโดนไล่เก็บ · เพลงพื้นหลัง (ถ้าเจ้านายอยากได้)

### กลุ่ม 3: ขัดเงา + ฉากเฉพาะหน้า
- [x] ~~ห้องปรับมูฟ (bg-moveset)~~ ✅ · ~~ห้องเตรียมตัว (bg-prep)~~ ✅ · ~~ฉากผล (bg-result)~~ ✅
- [ ] pulse ตึงเครียดตอนเวลาเหลือน้อยในห้องเตรียมตัว (ยังไม่ทำ)
- [ ] **Codex รีวิวโค้ดรอบสุดท้าย** ก่อนปิดงาน

---

## 🎨 asset ที่ยังไม่ wire
| asset | ที่ควรใช้ |
|---|---|
| `cat-smug/win/lose` · `employee-*` | home · สอนเล่น (ใช้ในฉากผลแล้ว) |

## 🎨 asset ครบแล้ว (2026-07-21)
crown · icon-dice · tutorial-moveset/pointer/duel — sync เข้าเกมแล้วทั้งหมด

**บทเรียนงานภาพจาก Codex:**
- Codex เจนบน **พื้นเขียว** แล้วคีย์ออกให้ — แต่มัก **เหลือขอบเขียว (fringe)** ต้องล้างเองก่อนใช้
- ใช้ `tools/chromakey.sh <in.png> <out.png>` (คีย์เขียว + despill) แล้วตรวจด้วยการทับพื้นม่วง:
  `magick out.png -background "#2b1a5e" -flatten check.png`
- แปลงเข้าเกม: `cwebp -q 88 -alpha_q 100 out.png -o app/public/assets/<หมวด>/<ชื่อ>.webp` แล้ว bump `V` ใน `data/assets.ts`

---

## 🔧 คำสั่งที่ใช้บ่อย

```bash
cd เกมที่3/app
npm run test              # เทส 110 ตัว
npm run build             # tsc + vite
node verify-flow.mjs      # เดิน 17 จอ วัด overflow (ต้องเสิร์ฟ dist ก่อน)
node verify-offround.mjs  # เทสดวลนอกรอบ

# เสิร์ฟ dist ทดสอบ (Playwright ต้องการ):
ln -sfn dist rps-arena && python3 -m http.server 8902
# แล้ว verify ชี้ localhost:8902/rps-arena/

# sync asset จาก Codex เข้าเกม:
cp theme-samples/assets-batch/XXX.webp app/public/assets/{icons,chars,scenes}/
# แล้วเพิ่มใน app/src/data/assets.ts + bump V (?v=N ล้าง cache)
```

**deploy:** push main → GitHub Actions → `ainzth45-commits.github.io/rps-arena/` อัตโนมัติ

## ⚠️ กฎที่ต้องจำ (พลาดมาแล้ว)
- **Codex: ห้ามวาดภาพด้วยโค้ด ต้องเจนภาพเท่านั้น** (ดู `02-art-direction.md`)
- **AI เจนตัวอักษรไทยผิดเสมอ** — ประกอบเองด้วย Chromium+Mitr-Bold (ดู `tools/`)
- **คะแนนเก็บเป็นจำนวนเต็มหน่วย 0.1** กัน float เพี้ยน
- **ห้ามใส่ชื่อจริงพนักงานลง source** (repo public)
